
<div id="flow">
  <div class="container">
    <div class="row">
      <div class="col-sm-12 text-center"><h2>New Flow n° <%= @flow.code %> for <%= ((Compagny.find(compagny_id = @flow.compagny_id)).name).upcase  %> </h2></div>
    </div>
    <div class="row form">
      <div class="col-sm-12">
       <%= simple_form_for @flow, url: flows_path do |f| %>
       <%= f.input :level, collection: @levels, prompt: "Select your stage", include_blank: false %>
       <%= f.input :name, label: 'Title' %>
       <%= f.input :description%>
       <%= f.input :deliverable, label: 'Nb of deliverable'%>
       <%= f.input :date_sale%>
       <%= f.button :submit %>
       <% end %>
     </div>
   </div>
 </div>
</div>


